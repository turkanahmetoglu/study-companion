services:
  db:
    image: postgres:16
    container_name: study-companion-db
    environment:
      POSTGRES_DB: studycompanion
      POSTGRES_USER: turkan
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: